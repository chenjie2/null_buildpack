#!/usr/bin/env bash

echo "========> do nothing"



echo `pwd`
echo `ls`

BUILD_DIR=$1
CACHE_DIR=$2
echo $BUILD_DIR
echo $CACHE_DIR

echo `ls $BUILD_DIR/*`
echo `ls $CACHE_DIR/*`


echo "<===============finsh"
exit 0
